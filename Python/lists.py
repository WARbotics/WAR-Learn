# Lets make myList varible by using the []
myList = ['one', 1, 'cheese']
# Lets get a item from the list
callingMyList = myList[2]
# This gives us "cheese"
# Note: That list start at 0 not 1
# You can also find the length of the list via 
print(len(myList))
# this would give you the int of 3 

